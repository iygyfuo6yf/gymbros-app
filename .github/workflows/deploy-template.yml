name: Deploy Template

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deployment placeholder
        run: |
          echo "Use deploy/render.yaml or deploy/docker-compose.prod.yml with your secrets configured."
